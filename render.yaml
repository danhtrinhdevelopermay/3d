services:
  - type: web
    name: image-to-voxel
    env: static
    buildCommand: npm run build
    staticPublishPath: ./dist
    envVars:
      - key: API_KEY
        sync: false
    routes:
      - type: rewrite
        source: /*
        destination: /index.html